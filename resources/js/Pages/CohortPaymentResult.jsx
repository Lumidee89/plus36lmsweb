import { Head, Link } from '@inertiajs/react';
export default function CohortPaymentResult({ enrolled, message, reference }) {
    return <main className="flex min-h-screen items-center justify-center bg-[#FFF7EC] p-6"><Head title="Cohort enrollment"/><section className="w-full max-w-lg rounded-3xl bg-white p-8 shadow-sm"><h1 className="text-2xl font-black text-teal-900">{enrolled ? 'You’re enrolled!' : 'Payment update'}</h1><p className="mt-4 text-slate-600">{message}</p><p className="mt-4 text-sm text-slate-500">Using the mobile app? Close this browser to return to the app.</p><p className="mt-4 break-all text-xs text-slate-400">Reference: {reference}</p><Link href="/dashboard?tab=available-courses" className="mt-6 block rounded-xl bg-teal-800 p-3 text-center font-bold text-white">Return to my dashboard</Link>{!enrolled && <button onClick={() => window.location.reload()} className="mt-3 w-full p-3 font-bold text-teal-800">Check payment again</button>}</section></main>;
}
